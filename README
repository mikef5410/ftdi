This source tree uses git submodules to manage sub-projects.

libmpsse is the only library I've modified. My mods are on the MF_MODS branch.

To initially populate: "git submodule init; git submodule update;"
